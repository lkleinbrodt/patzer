I want to write one (or many) blog posts about this project. So we should keep a running log here of everything we do to make it easy to remember. Basically any time we do something meaningful or interesting, we shoud write a short note here. (for example, making the model better, expanding training data, fixing a pesky bug, changing our eval system, etc.)

- **2026-05-04:** **Scrape → R2.** **`pipeline/scrape_lichess.py`** gained **`--push-r2`** (after each month: **`games_YYYY-MM.txt`**, stats, **`progress.json`**, **`scrape.log`**). **`launch.py train`** is the training entrypoint; legacy **`launch.py --config …`** inserts **`train`**. Pull on a workstation: **`python r2.py pull data/lichess_games`**. **`requirements.txt`**: added **`requests`** (used by scrape). *(Earlier **`launch.py scrape`** for Vast CPU was added then removed in favor of DigitalOcean — see **`pipeline/droplet_scrape.sh`**.)*

- **2026-05-04:** **`scrape_lichess.py --min-month YYYY-MM`** — drop dumps strictly before that month (string order). Use **`--min-month 2020-04`** when older months already exist locally (e.g. chewy); **`--min-month 2013-01`** (or similar) for full history.

- **2026-05-04:** **Lichess scrape → DigitalOcean droplet** — **`pipeline/droplet_scrape.sh`** (`sync` / `push-env` / `setup` / `run`) rsyncs the repo (skips **`data/`**), copies **`.env`**, installs **`wget`/`zstd`/python3** + minimal pip deps, runs **`scrape_lichess.py`** over SSH. **`CLAUDE.md`** updated.

- **2026-05-04:** Removed **`launch.py scrape`** / all Vast CPU scrape wiring from **`launch.py`**; **`launch.py`** is GPU training only.

- **2026-05-05:** **`pipeline/droplet_scrape.sh setup`** now creates **`~/patzer/.venv`** and **`pip install`s** scrape deps inside it (Ubuntu PEP 668 blocks system pip; **`python-chess`** must be installed for **`import chess`**). Run scrape with **`.venv/bin/python`**. **`pipeline/DROPLET_SCRAPE.md`** troubleshooting section.

- **2026-05-05:** **`pipeline/count_games_txt.py`** — default and **`--exact`** read **`games_YYYY-MM.stats.json`** **`kept_games`** when present. **`--elo-distribution`** uses **`elo_histogram`** (average ELO, same bins as **`parse_pgn.py`**) with a stated uniform-within-bin approximation for cutoffs inside a bin; falls back to reading **`games_*.txt` / `.txt.gz`** with the same average cutoff. Documented as not **`prepare.py --min-elo`**.

- **2026-05-05:** **High-ELO prepared splits — `prepare.py` + `count_games_txt.py`.** **`pipeline/prepare.py`** accepts **`--min-elo N`** (both players **≥ N**, same as **`filter_games.py`**). **`pipeline/count_games_txt.py --elo-distribution`** scans **`games_*.txt`** and prints how many games survive each ELO floor (default cutoffs **1800–2500** every **100**; **`--elo-low` / `--elo-high` / `--elo-step`** override). **`meta.json`** records **`min_elo_prepare`**.

- **2026-05-05:** **`train_patzer_v6.py` added** — same **16L / 16H / 768d** + WSD as v5; **`dataset = 'prepared_min_elo_2100'`** (~**1.73B** train tokens, **2100+** ELO). Output **`checkpoints/patzer_v6`**.

- **2026-05-06:** **`train.py` — post-cooldown min_lr tail.** `auto_cooldown` now continues training at `min_lr` after the WSD cooldown finishes instead of terminating. On cooldown completion, `evals_without_improvement` is reset to 0 and a `[wsd] cooldown complete` log line is printed; patience-based early stopping then governs the actual exit. `max_iters` is extended to `cooldown_end + (patience+5)*eval_interval` as a safety cap. Resume-safe: `_post_cooldown_phase` is pre-set `True` on resume so the reset doesn't re-fire mid-tail. Also: `ckpt.pt` auto-pulled from R2 on `init_from=resume` when missing locally (rank-0 pull + DDP barrier). **`train_patzer_v7.py`** created: `early_stop_min_iters=90k` (was 150k), `cooldown_iters=50k` (was 38k), same arch (16L/16H/768d) + 2100+ data.

- **2026-05-06:** **v6 post-cooldown continuation plan.** v6 used **WSD + `auto_cooldown=True`** and terminated after completing the linear decay to **`min_lr`**. If val loss is still improving at the end of cooldown, the practical continuation is a **new phase** resumed from `ckpt.pt` but **with a fresh LR policy** (e.g. hold a moderate constant LR by setting `decay_lr=False`, or switch schedules) rather than extending training at `min_lr` indefinitely.

- **2026-05-06:** **ELO cutoff counting now matches `prepare.py --min-elo`.** **`pipeline/count_games_txt.py`** gained **`--min-elo-distribution`** to count games by **min(WhiteElo, BlackElo) >= cutoff** (both players >= cutoff), using **`min_elo_histogram`** from sibling **`games_*.stats.json`** when present (fast) and falling back to scanning text dumps otherwise. **`pipeline/parse_pgn.py`** now writes that `min_elo_histogram` alongside the existing average-ELO histogram for future months.

- **2026-05-05:** **`launch.py` Vast search + remote CUDA preflight** — default query adds **`cuda_vers>=12.4`** (override with **`--min-cuda-vers 0`** to disable). Offer table includes a **CUDA** column (`cuda_max_good` / `cuda_vers`). Bootstrap script runs a **`torch.cuda.is_available()`** check in **`patzer/`** before R2 / `train.py` (exit **3** if no GPU).

- **2026-05-05:** **`launch.py` config preflight** — before `--instance` or renting, **`validate_train_config()`** checks `patzer/config/{name}.py` exists under the repo, compiles, and `exec`s with normal builtins (typos / syntax errors fail locally with **exit 2**). **`--list` / `--status`** unchanged. Optional **`.py`** suffix on `--config` is stripped.

- **2026-05-05:** **`train_patzer_v5.py` added** — **16L / 16H / 768d**, same **prepared** data as v4. **`model.get_num_params()` ≈ 116.5M** at init (not ~85M — vocab/block). **WSD** @ **6e-4**, **`auto_cooldown=True`**, **`cooldown_iters=38000`**, **`warmup_iters=5000`**, **`early_stop_min_iters=150000`**. Output **`checkpoints/patzer_v5`**.

- **2026-05-05:** **`pipeline/prepare.py` — stderr layout:** before each per-file log line, clear an in-place TTY progress line (`\r` + **EL** erase) so it no longer splices into the **Hash split · read** headers.

- **2026-05-05:** **`pipeline/prepare.py` — default `--workers`** capped at **4** (was **`cpu_count−1`**, e.g. 7 on 8-thread boxes). **`--workers`** can still be set higher explicitly.

- **2026-05-05:** **`pipeline/prepare.py` — further memory / tuning:** hash split no longer returns full game **lines** from workers (only precomputed **`split_val` + `token_ids`**), avoiding huge pickling/IPC of every line. **`--max-in-flight N`** overrides the default **`2 × workers`** cap on the bounded executor queue.

- **2026-05-05:** **`pipeline/prepare.py`** — Restored **`_bounded_map`** after **`ProcessPoolExecutor.map`** slipped back in. **`map`** eagerly consumes the full chunk iterator (submitting every chunk before work finishes), which blows RAM on large **`games_*.txt`** runs; **`_bounded_map`** keeps at most **`workers × 2`** futures in flight while preserving result order (boundary split safe).

- **2026-05-04:** **Synced local `~/Projects/lichess-bot` → `chewy:~/Projects/lichess-bot` via `rsync -avzL`** (excluded `.venv`, `__pycache__`, mac junk). **`homemade.py`** was a symlink to Patzer’s shim; **`-L`** copied the real file so Linux doesn’t point at a Mac-only path.

- **2026-05-04:** **`build_blog_assets.py` — fixed `VERSION_META` architecture for v1/v2** (was wrongly **12L/8H/256d**; configs are **6L/6H/384d**). **`patzer_page.html` hero:** training tokens **404B → 2.85B** (v4 train split per `train_patzer_v4.py`). Regenerated **`scripts/blog_assets/*.json`** + embedded **`window.BLOG_DATA`**.

- **2026-05-04:** **`MODELS.md` sync from R2 `metrics.jsonl` + local `evaluate.py leaderboard`.** Pulled `checkpoints/patzer_v{1–4}/metrics.jsonl`; DB still **7268** games — ladder tops unchanged (**v4@201** 1600, **v3@180** 1579, **v2@288** 1402, **v1@40** 1196). Doc fixes vs fresh metrics: **v3** global best val **1.510 @ 180k** (table had wrongly named 145k/1.514 as best); **v1** best **~1.841 @ 39k** (note had said ~30k); **v4** LM gain vs v3 **~0.009 nats** when comparing to v3’s true best; **v2** training table now lists best val **~1.650 @ 288k**.

- **2026-05-04:** **Lichess bot rotation + graceful shutdown.** All `bot/configs/patzer_v{1–4}.yml` set **`quit_after_all_games_finish: true`** (Ctrl-C / SIGINT finishes games first). New **`bot/cycle_bots.py`** runs each label for **`--dwell`** wall time (default **3600s**), sends **one SIGINT**, then waits (with **`NTFY_TOPIC`** alerts for early exits, internal errors, slow post-SIGINT shutdown, and operator Ctrl-C). With no CLI labels it **auto-discovers** every `patzer_*.yml` / `patzer_*.local.yml` under **`bot/configs/`**. **`deploy_bot.prepare_run()`** centralizes argv/env for `deploy_bot run` and the cycler.

- **2026-05-04:** **Bot configs — `matchmaking.challenge_filter: fine`.** All `bot/configs/patzer_v{1–4}.yml` now use **`fine`** instead of **`coarse`**, so a declined outgoing challenge applies a cooldown on the specific speed/mode/variant (per lichess-bot) rather than blocking that opponent entirely for a day.

- **2026-05-03:** **`MODELS.md` — v4 + unified eval refresh.** v4 marked completed (WSD phases). **Best val ~1.501 @ 201k** = **`weights_best.pt`**; training **stopped manually ~203k** (flat — confirmed). Eval table + ladder: `patzer_v4@201` **#1** (**1600 ± 11** vs `patzer_v3@180` **1579 ± 10**, Stockfish-anchored). Unified intro/table + v1–v3 numbers aligned to `results.db`; hardware v4 **50×128** ~10s eval. Follow-up: fixed accidental `~~` strikethrough / broken backticks in v4–v2–WSD bullets.

- **2026-05-03:** **Lichess dashboard — CPL-by-move smoothing.** Curve applies a **centered rolling mean** over neighboring bot move indices (**±5** moves where bins exist) before plotting; methodology + tooltip note it.

- **2026-05-03:** **Lichess dashboard — CPL-by-move.** Top chart plots **`avg_cpl`** vs bot move # (same **`curve_series`** SQL); title, methodology, Y-axis (**orange**), and tooltip copy updated. **`get_lichess_performance_snapshot`** docstring notes UI uses CPL.

- **2026-05-03:** **Lichess dashboard — eval-by-move low-n tail.** **`LichessMovePerformanceCharts`** draws each bot as **two Recharts lines**: solid while **`n ≥ 30`** at that move index, **dotted** from the first index where **`n < 30`** (with one overlapping anchor point for continuity). **`ChartMethodologyHint`** uses **`sections`** + **`footnote`** (titled blurbs, not one wall of text). Chart tooltips: header + column labels + color swatches; payload prefers **solid over dotted** when both exist; **· sparse n** on dotted-only rows.

- **2026-05-03:** **Lichess dashboard — chart tooltips.** **`ChartMethodologyHint`** no longer relies on the native **`title`** attribute (no click support, flaky on touch). It now opens a real **`role="tooltip"`** panel on **hover** or **click** (Escape / outside click to dismiss). **Recharts** tooltips on **`LichessMovePerformanceCharts`** get **`filterNull={false}`**, **`allowEscapeViewBox`**, higher **`wrapperStyle` z-index**, and **`overflow-visible`** on the chart shell so the library’s tooltip wrapper is not clipped.

- **2026-05-03:** **Lichess leaderboard — bot name → profile.** **`LichessAnalysis`** bot column links to **`https://lichess.org/@/{lichess_username}`** (new tab), same idea as bullet/blitz perf links.

- **2026-05-03:** **Lichess leaderboard — site bullet/blitz ratings.** `GET /api/lichess/stats` enriches each bot row with **`lichess_username`** (same rules as sync: defaults + **`PATZER_VN_USERNAME`**) and **`lichess_bullet_rating` / `lichess_blitz_rating`** from the public **`GET https://lichess.org/api/user/{username}`** profile (`perfs.*.rating`), cached ~120s server-side. **`LichessAnalysis`** table adds **Bullet** / **Blitz** columns (links to Lichess perf pages).

- **2026-05-03:** **Lichess dashboard — header Sync / Analyze.** Removed **`SyncPanel`** log stream UI; **`Layout`** shows compact **Sync** + **Analyze** on **`/lichess*`** with pills (**Syncing** / **Synced** / **Analyzing** / error truncate + `title`).

- **2026-05-03:** **Lichess dashboard — single tab.** Removed **`/lichess/performance`** (redirects to **`/lichess`**), **By move** nav, game history table, and **`LichessPerformance` / `LichessByMoveCharts`**. **`/lichess`** = short header + **Leaderboard** table + **Game performance** charts; methodology via **`(?)`** **`ChartMethodologyHint`**.

- **2026-05-03:** **`/api/lichess/performance` — per-bot series.** SQL now groups by **`bot_version`** (Patzer rows only); JSON **`curve_series`** / **`phase_series`** replace flat **`curve`** / **`phases`**. UI: one **eval** line per bot (CPL still in payload, not drawn); phase chart is **grouped bars per bot** with a **Blunder rate / Avg CPL** toggle. Shared **`LichessMovePerformanceCharts`** + **`botChartColors`**.

- **2026-05-03:** **Lichess Games page — CPL vs game # removed.** Static bots do not “improve” over game count, so the old **`/lichess/cpl_trend`** rolling-average chart was dropped (backend route + **`get_cpl_trend`** removed). **`LichessAnalysis`** now embeds **`LichessByMoveCharts`** under the game-history filters (same **`bot_version`** as the table): per-bot **eval** vs **Nth bot move** and phase bars (blunder % or avg CPL) from **`/api/lichess/performance`**.

- **2026-05-03:** **Lichess dashboard — “By move” performance page.** Frontend route **`/lichess/performance`**: charts for **`/api/lichess/performance`** (per-bot eval vs move index, phase bars with blunder/CPL toggle), bot filter, methodology copy; **`NavLink`** **`end`** on Lichess Games so nested path active states stay correct.

- **2026-05-03:** **Eval dashboard leaderboard — Patzer only.** `GET /api/eval/leaderboard` now returns **`ratings` only for `patzer_v*@*`** (same rule as summary stats); Stockfish anchors no longer appear in the local leaderboard table. `EvalLeaderboard` table styling simplified since every row is a model.

- **2026-05-03:** **Lichess dashboard cleanup + v2 username.** Removed debug **`/api/lichess/store-stats`** route and per-sync DB path log; **`patzer_v2`** Lichess export username defaults to **`patzer_v2b`** (`PATZER_V2_USERNAME` overrides). UI shows **`v1`…`v4`** via `botVersionLabel` (full id in `title` / filter values unchanged).

- **2026-05-03:** **Lichess dashboard — SQLite writes were never committed.** After switching to **`contextlib.closing()`** to avoid FD leaks, every connection exit called **`close()` without `commit()`**. In Python 3.12+ sqlite3, pending DML is **rolled back on close** (while `executescript()` in `init_db` still committed DDL), so sync logs showed in-memory work but **`lichess_games` stayed empty** — unrelated to `eval/results.db`. Fix: **`_db()` context manager** that **`commit()`** on success / rollback on error, then close.

- **2026-05-03:** **Lichess dashboard — SQLite path + import path (sync wrote, API read empty).** `lichess_games.db` used `Path(__file__).parent.parent` **without `.resolve()`**, so if `__file__` was ever cwd-relative the DB path was cwd-relative too — **background sync vs Flask requests could hit different files** (sync log showed rows; `/api/lichess/games` saw 0; analysis queue empty). Fix: **`DB_PATH = (...).resolve()`**, **`run.py` prepends `dashboard/` to `sys.path` first** so `backend` always resolves to `dashboard/backend`. **`upsert_game_with_moves`** now **deletes `lichess_moves` before `INSERT OR REPLACE`** on `lichess_games` (FK-safe re-sync). Startup prints **`[dashboard] Lichess DB path: …`**; analysis empty-queue log dumps **`lichess_store_stats()`**; **`GET /api/lichess/store-stats`** for manual checks.

- **2026-05-03:** **Lichess dashboard — analysis must not “complete” bad games.** `_analyze_game` logged PGN vs DB move-count mismatch but still **`upsert_game_aggregate` → `analyzed_at`**, permanently removing games from the analysis queue even with **zero `lichess_moves`**. Now **returns early** with no aggregate when move rows are missing or counts differ. Sync log adds **`(with PGN: N, move rows written: M)`** per bot; move parsing uses **`_parse_move`** (try UCI when it looks like UCI, else SAN).

- **2026-05-03:** **Lichess dashboard ingest — SAN vs UCI + player ids.** DB had **all `pgn` NULL and zero `lichess_moves`** because the API’s JSON **`moves` field is space-separated SAN** (`e4 d5 …`), not UCI — `Move.from_uci("e4")` failed on ply 1. Also **`players.*.user.id`** was ignored (code read nonexistent **`userId`**), so bot color was wrong. Fix: **`pgn_in_json=True`** + **`opening=True`** on export, parse moves with **`board.parse_san`**, store **`move.uci()`** in the moves table, optional **`initialFen` / chess960** board helper, correct **`_lichess_player_id`**. Re-sync to backfill.

- **2026-05-03:** **Lichess dashboard — post-sync analysis visibility + UI.** Auto-analysis did run after sync, but **`logger.info` from `analysis.py` was invisible** in the default Flask dev console, so it looked like nothing happened. Added **`[lichess_analysis]` stdout** (same idea as sync), a **`[lichess_sync] Scheduling Stockfish analysis…`** line, **`GET /api/lichess/analyze/status`**, and an **Analyze games** control on the dashboard (now header **Analyze** on Lichess routes; polls until the batch finishes, then invalidates Lichess queries).

- **2026-05-03:** **Dashboard `lichess_games.db` “unable to open database file” during sync.** Root cause was not SQLite threading: **`with sqlite3.connect(...) as con` does not close the connection** (only commits/rolls back). Sync opened two connections per game (`upsert_game` + `insert_moves`) without closing until GC → **EMFILE / too many open files**, then misleading SQLite errors. Fix: wrap every use in **`contextlib.closing`** in `dashboard/backend/db.py`. Follow-up: **`upsert_game_with_moves`** — Lichess sync writes game + moves in one connection (fewer round-trips); empty move list now clears stale `lichess_moves` rows on re-sync.

- **2026-05-03:** **`play_game(..., return_move_history=True)`** — optional third return value: full UCI move list (useful for tests / tooling).

- **2026-05-02:** **`bot/configs/patzer_v2.yml`** — aligned with **v3** (concurrency 2, rated-only challenges, `min_think_ms` / `think_jitter_ms`, matchmaking `challenge_timeout: 0`) but **`patzer_checkpoint: checkpoints/patzer_v2/weights_best.pt`**. Replaced broken `patzer_v/` path and older settings.

- **2026-05-02:** **`bot/configs/patzer_v4.yml`** — same layout as **v3** (`checkpoints/patzer_v4/weights_best.pt`, think/jitter, rated-only, `challenge_timeout: 0`, v4 greeting). **`patzer_v{1,2,3}.yml`** headers already matched their versions; kept that convention for v4.

- **2026-05-02:** **`--visualize` for eval.** `eval/eval_viz.py`: split window — left (~460px) monospace scroll log (tone-colored lines mirroring **`_print_sf_*`**, **`_print_match_*`**, standings); right = live board + status. **`play_game(..., on_ply=)`** refreshes the board each half-move; **`viz.log()`** refreshes on each logged line (between games too). Stockfish summary prints before **`viz.close()`**. No game loop / negligible overhead vs engines.

- **2026-05-02:** **`eval/evaluate.py gauntlet` mode.** Interactive (or `--no-prompt`) gauntlet: pass a challenger checkpoint, see Patzer-only leaderboard candidates (excluding the challenger’s player label), pick opponent ranks like `rr-leaderboard`, then run sequential **head2head** matches—challenger vs each opponent only (no games among opponents). Default **`--games 50`** per pairing; shared **`--min-games`**, **`--seed`** (per-match offset), **`--limit`**. Refactored **`_parse_rank_list`** and **`_print_patzer_candidate_table`** for reuse with `rr-leaderboard`.

- **2026-05-02:** **R2 / training upload hardening.** All pushes use **`put_object`** (no `upload_file` / S3Transfer nested threads). Added **`flush_r2_uploads()`** — noop flush via single-worker executor — called when the training loop exits and from **`_final_r2_sync`** atexit so async uploads complete before teardown. **`push_async`**: queue-depth warning when >5 pending, **`submit` failure → synchronous foreground fallback** (never silent drop). Config **`r2_async_uploads`** (default True): set False to block on every large upload. `push_file` / CLI **`push_dir`** now use the same streaming put path.
- **2026-05-02:** `eval/engine.py` — **vectorized legal-move mask** (`_apply_legal_move_mask`) and **`_MoveTokenCache`** for incremental UCI→token ids (append-only play). Regression suite `eval/test_patzer_engine.py` (unittest) compares mask/top-k/cache/`get_move` to reference implementations; run `python -m unittest eval/test_patzer_engine.py -v` from repo root.
- **2026-05-02:** `patzer/r2.py` — **cached** boto3 client (thread-safe) with **adaptive retries** and longer read timeout; **`pull_file` / `pull_dir`** skip only when **`is_fresh`** (ETag sidecar matches R2), otherwise re-download; **push** paths write **`.r2meta`** after upload so pulls can verify freshness without `--force`.
- **2026-05-02:** Training / config / checkpoints — `patzer/configurator.py` parses `--key=value` with `split('=', 1)` and allows `None` defaults to accept any typed override. `patzer/train.py`: `torch.amp.GradScaler('cuda')` (CUDA fp16 only); cosine LR uses a safe denominator + clamped decay ratio; shared `patzer/checkpoint_util.load_checkpoint` (prefer `weights_only=True` + `GPTConfig` safe globals, fallback for legacy). `eval/engine.py` and `patzer/sample.py` use the same loader; `sample.py` context crop matches engine (preserve result token only when present). `CODE_REVIEW.md` deduped.
- **2026-05-02:** `pipeline/filter_games.py` — CLI now **`--allow-bots`** and **`--include-variants`** (opt-in); old `--no-bots` / `--standard-only` never worked as documented (`store_true` + `default=True`). `parse_pgn.py` — progress log moved into **`flush_game()`** so it fires once per N **games**, not once per PGN line. `CODE_REVIEW.md` trimmed to living backlog.
- **2026-05-02:** Replaced stale `patzer/sample.py` (nanoGPT/tiktoken) with a chess-aware sampler: `ChessTokenizer`, legal-move masking, `conditioning` matching eval, optional UCI `start` / `FILE:`, `weights_best.pt` → `ckpt.pt` fallback, vocab_size check. `CODE_REVIEW.md` trimmed to a living backlog; `CLAUDE.md` sample commands updated (`--out_dir=../checkpoints/...` when cwd is `patzer/`).
- **2026-05-02:** **v4 Phase 1 complete; Phase 2 (cooldown) created and launched.** Phase 1 ran WSD with `cooldown_start_iter=None` (constant LR after warmup) until early stopping at iter 145001 (25 evals without ≥0.001 improvement). `ckpt.pt` at iter 140000, val 1.6548, `evals_without_improvement=20`. Created `train_patzer_v4_cooldown.py`: resumes from that checkpoint, `cooldown_start_iter=140000` (decay starts immediately), `cooldown_iters=30000`, `early_stop_patience_evals=0` (disabled — checkpoint had 20/25 patience consumed, would have re-triggered in 5 evals). Phase 2 ran to 170k but val was still dropping linearly, so extended to 220k via `--max_iters=220000`.
- **2026-05-02:** **`auto_cooldown` added to `train.py`.** New WSD flag. When `True`, instead of stopping when early stopping fires, training sets `cooldown_start_iter=iter_num`, extends `max_iters` by `cooldown_iters`, resets patience, and decays LR to `min_lr` — all in one job. `cooldown_start_iter` is now saved in `ckpt.pt` so mid-cooldown resumes restore the correct decay curve. Eliminates the need for manual two-phase configs going forward.
- **2026-05-02:** **W&B resume bug fixed.** `wandb.log({...}, step=iter_num)` was passing explicit step values, which W&B enforces as monotonically increasing. Resuming from an earlier checkpoint than where a run crashed caused all logs to be silently dropped. Fix: removed `step=iter_num`. W&B's internal counter auto-increments (always monotonic); x-axis is driven by `"iter"` via `define_metric`.
- **2026-05-02:** **atexit R2 final-sync error fixed.** Two attempts: (1) `push_file` → `push_async` didn't help — `_executor` was already shut down by the time atexit fired regardless of LIFO ordering. (2) Added `push_file_threadsafe` to `r2.py` using `client.put_object` (single HTTP PUT, no S3Transfer thread pool, no threads at all) — unconditionally safe in atexit context.
- **2026-05-02:** **Metrics R2 sync silently blocked by large weights uploads.** `push_async` uses `max_workers=1`. When a 160MB `weights_best.pt` upload hangs, all queued `metrics.jsonl` uploads pile up behind it — logs show pushes happening but R2 never updates. Fix: `metrics.jsonl` now uses `push_file_threadsafe` (synchronous, <100ms for a ~30KB file). Large files (weights, ckpt) stay async.
- **2026-05-02:** **v4 WSD retrospective.** Stable phase @ 6e-4 plateaued ~145k iters (val 1.6566 at early stop); 30k linear cooldown to 1e-5 dropped val to 1.507 — ~10× faster per iter than the late stable phase. Continued at min_lr for another ~30k iters → very slow drift to 1.501 (~0.0002 nats/1k iters, same order as late stable): approaching basin floor with diminishing returns, not a hard wall. The dramatic cooldown drop is *expected* WSD behavior (DeepSeek/MiniCPM): the stable phase accumulates representation work that cooldown realizes; it is **not** evidence the constant-LR phase was wasted. Bigger signal: 3.3× data → only +0.013 nats over v3 (1.514 → 1.501). v3's "data-bound" gen-gap signal has flipped — **v4 is capacity-bound at 40M params**. v5 should scale architecture, not chase more data or schedule micro-tuning. v5 schedule defaults: keep WSD with constant LR=6e-4, flip `auto_cooldown=True` (avoids the manual two-phase juggling that caused the iter-140k/160k restart blips), bump cooldown ratio from ~20% to ~25–30% of stable length. Full table + lessons in `MODELS.md` v4 section.

- **2026-05-05:** **Scrape pipeline speed overhaul — fast parse mode + multiprocessing.** Rewrote `pipeline/parse_pgn.py` with two speedups: (1) **Fast mode (default):** skips `chess.pgn.read_game()` PGN tree builder and directly extracts SAN tokens → `board.parse_san()` loop. Avoids Game tree construction, comment/NAG/variation handling overhead. ~30% faster per game. (2) **Parallel workers (`--workers N`, default `cpu_count-1`):** main thread reads PGN stream and batches games (2000/batch default), dispatches to `multiprocessing.Pool.imap_unordered`, workers do the CPU-heavy SAN→UCI conversion, main thread writes results. On 4 cores: ~2x speedup; scales linearly with cores. Combined on 8-core machine: ~5x total vs old single-threaded validated pipeline. Use `--validate` to restore original strict `chess.pgn.read_game()` behavior. `scrape_lichess.py` passes `--workers` and `--validate` through to parse_pgn.py.

- **2026-05-05:** **Compressed monthly games outputs (`.txt.gz`) end-to-end.** `pipeline/parse_pgn.py` now writes gzip transparently when `--output` ends with `.gz` (stats default stays stable as `*.stats.json`, not `*.txt.gz.stats.json`). `pipeline/scrape_lichess.py` now defaults monthly outputs to `games_YYYY-MM.txt.gz` (use `--no-compress` to keep `.txt`), and R2 push looks for either `.txt.gz` or `.txt`. `pipeline/prepare.py` now glob-defaults to `games_*.txt*` and automatically reads either plain text or gzip input.

- **2026-05-05:** **`pipeline/compress_games_txt.sh`** — for legacy plain **`games_*.txt`**: **`gzip -6 -k`** only if **`games_*.txt.gz`** is missing; require **`games_*.stats.json`** and **`gzip -dc` line count == `kept_games`** before **`rm`**’ing the **`.txt`**; nonzero exit if anything skips/errors (checksum/script rerun stays safe).

- **2026-05-05:** **`pipeline/prefetch_lichess.sh`** — optional parallel downloader to overlap **network** with **CPU parse**. Watches `data/lichess_games/progress.json` and any already-downloaded `lichess_db_standard_rated_YYYY-MM.pgn.zst` files; keeps exactly **one month ahead** downloaded via resumable `wget -c` with low I/O priority (`nice` + `ionice`). Uses a lockfile to prevent multiple prefetchers per output dir.


## 2026-05-06

- Ran cross-dataset eval: `checkpoints/patzer_v6/weights_best.pt` on v5 validation set (`data/prepared/val.bin`). Used `eval/val_loss.py` on MPS (float32), 50 iters, batch 32. Result: mean val loss 1.4640 (std 0.0262). Checkpoint-reported v6 val loss was 1.3960 at iter 217000 (on its own v6 val).

- **`scripts/patzer_page.html` + `scripts/build_blog_assets.py`:** Extended **`VERSION_META`** through **v5** and **v6** (aligned with **`MODELS.md`**). Hero **lichess Elo** uses the higher of **bullet** vs **blitz** with a matching subtitle (tie shows **blitz · bullet**). **Games played** copy now says all opponents (counts remain **`count.all`** from Lichess — all finished games on those accounts). Added **`training_tokens_short`** per version for hero metrics; flagship bot/challenge/footer lichess link follow the latest row in **`models.json`**. Regenerated **`scripts/blog_assets/*.json`** and re-embedded **`window.BLOG_DATA`**.

- **`scripts/patzer_page.html` — live Lichess game counts:** **`POST /api/users`** omits **`count`** (only slim **`perfs`** etc.). Switched to sequential **`GET /api/user/{username}`** per bot so **`count.all`** is present; fixes blank **games played** / leaderboard **games** column.

- **`scripts/patzer_page.html`:** Central **`fs(n) => round(n × 1.1)`** scales JSX **`fontSize`** and **`Mono`/`LiveDot`/`H2`** sizes (~10% larger copy site-wide).

- **`scripts/patzer_page.html`:** Main column **`maxWidth` 780 → 960**; horizontal **`padding`** tightened (**`clamp(14px, 2.5vw, 28px)`**) so the layout uses more viewport width.

- **`scripts/patzer_page.html`:** **`blog_assets/chess.png`** as favicon + hero mark beside the title; footer credit link to [Flaticon / VectorPortal chess icons](https://www.flaticon.com/free-icons/chess).
